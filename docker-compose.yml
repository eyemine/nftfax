services:
  nftfax:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_PRIVY_APP_ID=${NEXT_PUBLIC_PRIVY_APP_ID}
        - NEXT_PUBLIC_NFTMAIL_URL=${NEXT_PUBLIC_NFTMAIL_URL}
        - NEXT_PUBLIC_WORKER_URL=${NEXT_PUBLIC_WORKER_URL}
    container_name: nftfax-nftfax-1
    environment:
      - WORKER_SECRET=${WORKER_SECRET}
      - WEBHOOK_SECRET=${WEBHOOK_SECRET}
      - NFTMAIL_WEBHOOK_SECRET=${NFTMAIL_WEBHOOK_SECRET}
      - PINATA_JWT=${PINATA_JWT}
      - IRYS_PRIVATE_KEY=${IRYS_PRIVATE_KEY}
    ports:
      - "3002:3000"
    restart: unless-stopped
