import DrawClient from './DrawClient';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Prize Draw — NFTFAX',
};

export default function DrawPage() {
  return <DrawClient />;
}
