import VerifyClient from './VerifyClient';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Verify Prize Draw — NFTFAX',
};

export default function VerifyPage() {
  return <VerifyClient />;
}
